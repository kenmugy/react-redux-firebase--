const initialState = {};

const projectReducer = (state = initialState, action) => {
  //   switch (action.type) {
  //     case '':
  //       return {};
  //     case '':
  //       return {};
  //     default:
  //       return state;
  //   }
  return state;
};

export default projectReducer;
